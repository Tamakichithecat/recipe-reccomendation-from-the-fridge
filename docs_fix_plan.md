# RecipeRecommender 修正方針とタスク

## 1. ゴール再定義
- **現状**: 「一致食材数が多いレシピ」を選んでいる。
- **目標**: 「実際に支払う総額（不足分購入費 + 既存材料を使う前提コスト）が最小」のレシピを選ぶ。

### 完了条件
- レシピ選定基準が `matchCount` ではなく `totalCost` 最小になっている。
- 買えないケース（店舗在庫なし）が明示的に判定される。
- 数量不足を正しく価格計算できる。

---

## 2. 優先順位付きの修正方針

### P0（最優先）: ビルド不能リスクの解消
1. `PantryViewModel` の `objectWillChange` 手動宣言を削除する。
2. `@Published` ベースの通常実装に戻し、初期化エラーを防ぐ。

### P1: 推薦ロジックを「最安」中心に再設計
1. 全レシピを走査し、各レシピごとに以下を計算する。
   - 不足食材（不足量つき）
   - 不足食材を全て買える最安店舗
   - 総額（`baseCost + 不足分価格`）
2. 「購入不可レシピ」を候補から除外する（または別枠表示）。
3. 最終的に `totalCost` が最小のレシピを採用する。

### P1: 不足数量に基づく価格計算
1. `IngredientRequirement` と在庫との差分で不足量を算出する。
2. 価格データを「単位量あたり価格」か「1パック価格」か定義し、計算式を明確化。
3. 現状の `1食材=1回加算` を廃止する。

### P2: データ整合性の改善
1. 同名食材追加時は新規追加ではなく数量加算する。
2. 単位不一致（例: g と 本）検知時の扱いを定義する。
3. 入力バリデーション（数量>0、空白除去、異常値拒否）を追加する。

### P2: UI/UX改善
1. 「購入不可」の場合に明示メッセージを出す。
2. 不足食材表示を「必要量 / 保有量 / 不足量」で表示する。
3. 計算根拠（どこで何をいくらで買うか）を表示する。

### P3: 将来拡張の足場
1. `MockData` 依存を `Repository` 抽象へ切り出す。
2. 位置情報（CoreLocation）と店舗候補取得のIFを追加する。
3. READMEの要件と実装差分を埋めるロードマップを記載する。

---

## 3. 実装タスク（チケット化可能な粒度）

## Epic A: 安定稼働
- [ ] A-1 `PantryViewModel` から `objectWillChange` 手動宣言を削除
- [ ] A-2 ビルド確認（ローカル/Xcode CI）

## Epic B: 最安ロジック
- [ ] B-1 不足量計算ヘルパーを追加（在庫との差分を返す）
- [ ] B-2 店舗ごとの購入可能判定を追加
- [ ] B-3 レシピ評価関数 `evaluate(recipe, pantry, stores)` を追加
- [ ] B-4 `suggestRecipe` を「全レシピ評価→最小総額選択」に置換
- [ ] B-5 購入不可時の戻り値仕様（`unavailableReason` 等）を追加

## Epic C: データ品質
- [ ] C-1 同名食材の数量マージ実装
- [ ] C-2 単位不一致時のルール実装（変換/警告/拒否）
- [ ] C-3 入力バリデーション追加

## Epic D: 表示改善
- [ ] D-1 不足食材表示を `必要/保有/不足` に変更
- [ ] D-2 購入不可メッセージ表示
- [ ] D-3 合計想定額の内訳表示（baseCost + 購入費）

## Epic E: テスト
- [ ] E-1 ロジックユニットテスト追加（最安選定、同額時距離優先）
- [ ] E-2 購入不可ケーステスト追加
- [ ] E-3 同名食材マージテスト追加
- [ ] E-4 数量不足計算テスト追加

---

## 4. 受け入れ基準（Definition of Done）
1. 「一致数最多」ではなく「総額最小」のレシピが選ばれる。
2. 不足数量に比例して購入費が増減する。
3. 全店舗で揃わない場合、購入不可がUIで分かる。
4. 同名食材複数入力時に在庫が合算される。
5. 主要ロジックがテストで担保される。

---

## 5. 推奨実施順（1スプリント想定）
1. A（安定稼働）
2. B（最安ロジック）
3. E-1〜E-2（回帰防止）
4. C（データ品質）
5. D（表示改善）
6. E-3〜E-4（仕上げ）
